# .platform.app.yaml
name: app
type: php:8.2
disk: 1024

build:
  flavor: none

variables:
  env:
    APP_ENV: "prod"
    APP_DEBUG: "0"
    # Conserver la variable CORS, bien qu'elle ne soit pas la cause de l'erreur 500
    CORS_ALLOW_ORIGIN: "^https?://([^/]+\\.)?main-bvxea6i-rm5gtsnixyao4\\.fr-3\\.platformsh\\.site$"
    # Définition de la relation persistante. Le build hook va forcer sa lecture.
    MONGODB_URL: "value-from-relation:mongodb.url"
    MONGODB_AUTH_SOURCE: "value-from-relation:mongodb.auth_source"
    MONGODB_DB: "value-from-relation:mongodb.database"

web:
  locations:
    "/":
      root: "public"
      passthru: "/index.php"
      scripts: true
      allow: true

relationships:
  database: "db:mysql"
  mongodb: "mongodb:mongodb"

dependencies:
  php:
    composer/composer: "^2"

mounts:
  "/var": "shared:files"

hooks:
  build: |
    set -e
    composer install --no-dev --no-progress --prefer-dist --optimize-autoloader --ignore-platform-req=ext-mongodb

    # 💥 COMMANDE CRITIQUE : Génère un fichier var/cache/.env.local.php au build 
    # avec les valeurs réelles de MONGODB_URL, garantissant leur persistance au runtime.
    php vendor/bin/platformsh-config inject-env --prefix=MONGODB_ --relation=mongodb --output-file=var/cache/.env.local.php

  deploy: |
    set -e
    # Le cache clear s'exécute MAINTENANT que le fichier .env.local.php est présent
    # et que la variable MONGODB_URL est donc disponible.
    php bin/console cache:clear --env=prod --no-debug
    php bin/console cache:warmup --env=prod
    rm -rf var/log/*

runtime:
  extensions:
    - gd
    - intl
    - pdo
    - pdo_mysql
    - opcache
    - mongodb
