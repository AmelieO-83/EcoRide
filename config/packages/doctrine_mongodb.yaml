# config/packages/doctrine_mongodb.yaml
doctrine_mongodb:
  connections:
    default:
      url: "%env(resolve:MONGODB_URL)%"

  default_database: "%env(resolve:MONGODB_DB)%"

  document_managers:
    default:
      auto_mapping: true
